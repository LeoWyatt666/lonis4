Header set X-Content-Type-Options: nosniff
Header set X-Frame-Options: DENY
Header set X-XSS-Protection: "1; mode=block"

<Files .*>
    Order Allow,Deny
    Deny from all
</Files>

<FilesMatch ".(htaccess|htpasswd|ini|phps|fla|psd|log|sh|yaml|json|lock)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

<IfModule mod_rewrite.c>
    Options -MultiViews
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    # RewriteRule ^(.*)$ index.php [QSA,L]
    RewriteCond $1 !^lonis4/public/
    RewriteRule (.*) /lonis4/public/index.php [QSA,L]
</IfModule>

<IfModule !mod_rewrite.c>
    <IfModule mod_alias.c>
        RedirectMatch 302 ^/lonis4/$ /lonis4/index.php/
    </IfModule>
</IfModule>